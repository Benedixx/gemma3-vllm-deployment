[project]
name = "llm-deployment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12"
dependencies = [
    "tiktoken>=0.11.0",
    "uuid>=1.30",
    "vllm>=0.10.1.1",
]
